# Worker Pools

Worker pools let you parallelize work with a fixed number of goroutines consuming from a jobs channel and writing to results.

Example (trade validation): see `main.go` in this folder.
